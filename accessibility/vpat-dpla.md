# Voluntary Product Accessibility Template (VPAT®) 2.4 Rev (INT)
## Digital Public Library of America — dp.la

---

> ## ⚠️ IMPORTANT DISCLAIMER
>
> **This is an UNOFFICIAL DRAFT document generated by artificial intelligence. It has NOT been reviewed, approved, or verified by DPLA staff, a certified accessibility professional, or any human expert. It MUST NOT be published, distributed externally, or used for procurement purposes without thorough human review and validation.**
>
> ### How This Report Was Generated
>
> This report was produced on **2026-03-10** by an AI assistant (Anthropic Claude, claude-sonnet-4-6) using the following methods:
>
> 1. **Static code analysis** of the `dpla/dpla-frontend` GitHub repository, examining React/Next.js components, stylesheets, and page files for accessibility patterns, ARIA usage, semantic HTML, form labeling, and media handling.
> 2. **Automated browser inspection** via injected JavaScript on the live dp.la homepage and search results page (`/search?q=photography`), checking for landmark regions, heading structure, image alt attributes, form labels, link names, and computed color values.
> 3. **Google site search** (`site:dp.la accessibility`) to identify any existing accessibility documentation.
>
> ### What This Report Cannot Determine
>
> - **Screen reader compatibility**: No testing was performed with JAWS, NVDA, VoiceOver, or any other assistive technology.
> - **Keyboard navigation**: No manual keyboard-only navigation testing was performed.
> - **Color contrast**: Contrast ratios were estimated from computed CSS values but not verified with a calibrated contrast analysis tool across all page templates and themes.
> - **Dynamic interactions**: Only static snapshots of pages were analyzed; complex user flows (e.g., multi-step search filtering, list management) were not tested interactively.
> - **Third-party content**: Embedded content from partner institutions or external services was not evaluated.
> - **PDF accessibility**: Source PDFs linked from the site were not evaluated.
> - **All pages**: Only the homepage and search results page were directly inspected; findings on other page types (item pages, exhibitions, primary source sets, etc.) are inferred from codebase analysis.
>
> This report should be treated as a **starting point for a formal accessibility audit**, not a substitute for one.

---

## Product Information

| Field | Value |
|---|---|
| **Product Name** | Digital Public Library of America — Public Portal |
| **Product Version** | Not versioned (live web application) |
| **Product URL** | https://dp.la |
| **Report Date** | 2026-03-10 |
| **Report Version** | Draft 0.1 (AI-generated, unreviewed) |
| **Contact** | info@dp.la |
| **Codebase** | https://github.com/dpla/dpla-frontend |
| **VPAT Version** | VPAT® 2.4 Rev (INT) |

---

## Applicable Standards / Guidelines

This report covers conformance with the following standards:

- **Web Content Accessibility Guidelines (WCAG) 2.1**, Levels A and AA
- **Revised Section 508** standards (United States): 36 CFR Part 1194

---

## Terms

| Term | Definition |
|---|---|
| **Supports** | The functionality meets the criterion without exception. |
| **Partially Supports** | Some functionality meets the criterion; some does not. |
| **Does Not Support** | The majority of functionality does not meet the criterion. |
| **Not Applicable** | The criterion is not relevant to this product. |
| **Not Evaluated** | The criterion was not evaluated in this assessment. |

---

## Evaluation Methodology Summary

- **Automated browser inspection** via JavaScript on live pages (homepage, search results)
- **Static source code analysis** of the dpla-frontend repository
- **No** screen reader testing
- **No** manual keyboard-only navigation testing
- **Partial** color contrast analysis (computed CSS values, not full tool-based verification)
- Pages directly tested: Homepage (`dp.la`), Search results (`dp.la/search?q=photography`)

---

## WCAG 2.1 Report

### Table 1: Success Criteria, Level A

| Criterion | Conformance Level | Remarks and Explanations |
|---|---|---|
| **1.1.1 Non-text Content** | Partially Supports | **Fail — collection thumbnails:** All 20 thumbnail images on the search results page use `alt=""` (empty string). These images represent distinct collection items and are not decorative; screen reader users receive no information about the items. `alt=""` is appropriate only for purely decorative images. **Pass — homepage illustrations:** The 4 images on the homepage carry descriptive alt text. **Fail — DPLA logo SVG:** The `DplaLogoWhite` SVG in the page header has no `aria-label` or text alternative. **Fail — footer icon links:** Bluesky and YouTube icon links in the footer have no accessible name (no text content, no `aria-label`, and no `alt` on child images). |
| **1.2.1 Audio-only and Video-only (Prerecorded)** | Not Evaluated | A `VideoPlayer` and `AudioPlayer` component exist in the codebase. No video or audio was present on pages tested. Code review shows no transcript support is implemented in either component. Full evaluation requires testing pages that contain media content. |
| **1.2.2 Captions (Prerecorded)** | Does Not Support | The `VideoPlayer` component (`components/shared/mediaViewers/VideoPlayer.js`) renders a `<video>` element with no `<track>` element. There is no mechanism to deliver WebVTT captions for any video content on the site. |
| **1.2.3 Audio Description or Media Alternative (Prerecorded)** | Not Evaluated | Not tested. No video content was present on tested pages. |
| **1.3.1 Info and Relationships** | Partially Supports | **Fail — header landmark:** The global page header uses `<div class="header">` rather than `<header>`. Screen reader users navigating by landmark cannot locate the banner region. **Partial — main landmark:** The search results page correctly uses `<main id="main">`. The homepage uses `<div id="main" role="main">`, which provides the ARIA role but not the semantic HTML element. **Fail — navigation landmark:** No `<nav>` or `role="navigation"` element is used for the primary site navigation. **Fail — table headers:** The contact information table (`components/ContactComponents/MoreWaysToContact`) uses `<th scope="col">` elements with no text content. **Fail — newsletter checkbox:** The "I prefer USPS mail" checkbox in the newsletter signup has no associated label (no `<label>`, no `aria-label`, no `aria-labelledby`). **Pass:** `<footer>` semantic element is present. Accordion components use proper `aria-controls` and `aria-expanded`. |
| **1.3.2 Meaningful Sequence** | Supports | Visual reading order appears consistent with DOM order on tested pages. No reordering via CSS that would create a meaningfully different sequence was observed. |
| **1.3.3 Sensory Characteristics** | Supports | No instructions that rely solely on sensory characteristics (shape, color, size, position, sound) were identified. |
| **1.4.1 Use of Color** | Supports | No instances were identified where color is the only means of conveying information. |
| **1.4.2 Audio Control** | Not Applicable | No audio content plays automatically. |
| **2.1.1 Keyboard** | Partially Supports | **Fail — carousel navigation buttons:** Navigation arrows in carousels (exhibitions, primary source sets) use `tabindex="-1"`, making them unreachable by keyboard. Carousel content is inaccessible to keyboard-only users unless alternative navigation exists. **Fail — download link:** The "Download list" link in `ListView` uses an `onClick` handler on an `<a>` element with no `href`. This element does not receive activation from the Enter key as a link, and does not behave as a button. **Pass:** Form fields, accordion filters, mobile menu toggle, pagination links, and modal dialogs are keyboard operable. |
| **2.1.2 No Keyboard Trap** | Supports | Modal dialogs use `react-aria-modal`, which correctly traps focus within the dialog and restores focus on close. No other keyboard traps were identified. |
| **2.1.4 Character Key Shortcuts** | Not Applicable | No single-character keyboard shortcuts were identified. |
| **2.2.1 Timing Adjustable** | Not Applicable | No session timeouts or time limits were identified in the client-side codebase. |
| **2.2.2 Pause, Stop, Hide** | Supports | No auto-playing moving content was observed on tested pages. |
| **2.3.1 Three Flashes or Below Threshold** | Supports | No flashing content was identified. CSS transitions use smooth, slow easing (0.15s) well below the 3Hz threshold. |
| **2.4.1 Bypass Blocks** | Partially Supports | A "Skip to Main Content" link is implemented (`components/shared/SkipToContent`) and is the first focusable element on all pages. However, on the homepage the skip link target (`<div id="main">`) does not have `tabindex="-1"`, which may prevent programmatic focus from being moved to the main content area in some browsers after activation. On the search results page, `<main>` is used correctly. |
| **2.4.2 Page Titled** | Supports | Page titles are dynamically generated and descriptive. Examples observed: "Digital Public Library of America" (homepage), "photography \| Search Results \| DPLA" (search results). Titles follow a consistent pattern via `DPLAHead` component. |
| **2.4.3 Focus Order** | Not Evaluated | Not tested via keyboard navigation. Logical focus order is expected from DOM structure, but interactive components (modals, carousels, drawers) were not keyboard-tested. |
| **2.4.4 Link Purpose (In Context)** | Partially Supports | **Fail — footer icon links:** Bluesky and YouTube icon links provide no accessible name. Their purpose cannot be determined by link text, accessible name, or programmatic context. **Pass:** Most other links have descriptive text. Search result item titles serve as link text. Pagination uses "Previous" / "Next" text. |
| **2.5.1 Pointer Gestures** | Supports | No functionality requires multi-point or path-based gestures. |
| **2.5.2 Pointer Cancellation** | Supports | No issues identified with pointer event cancellation behavior. |
| **2.5.3 Label in Name** | Partially Supports | Most visible labels match accessible names. The mobile menu button has visible text "ShowMenu" (concatenated, no space) which may not match how screen readers announce it. Full audit needed. |
| **2.5.4 Motion Actuation** | Not Applicable | No functionality relies on device motion or user motion. |
| **3.1.1 Language of Page** | Supports | The HTML element includes `lang="en"` set in `pages/_document.js`. |
| **3.2.1 On Focus** | Supports | No unexpected context changes on focus were identified. |
| **3.2.2 On Input** | Supports | No unexpected context changes on input were identified. |
| **3.3.1 Error Identification** | Does Not Support | The contact form (`components/ContactComponents/ContactForm`) validates fields but provides no accessible error feedback when submission is blocked. The `handleSubmit` function returns silently when `formIsValid()` is false. No error messages, `aria-invalid` attributes, or `aria-describedby` references to error text are set. Users (particularly screen reader users) receive no indication of which fields are missing or invalid. |
| **3.3.2 Labels or Instructions** | Partially Supports | **Pass:** Contact form fields use `<label>` elements with `htmlFor` associations and include "(required)" text. The search input has `aria-label="Search the collection"`. **Fail:** The newsletter subscription checkbox has no label. The newsletter email field uses `aria-label` but no visible `<label>`. |
| **4.1.1 Parsing** | Not Evaluated | HTML validation was not performed. |
| **4.1.2 Name, Role, Value** | Partially Supports | **Pass:** Accordion uses `aria-expanded` and `aria-controls`. Modals use `react-aria-modal`. Mobile menu toggle uses `aria-expanded`. The contact form success message uses `aria-live="assertive"`. **Fail:** DPLA logo SVG has no accessible name. Newsletter checkbox has no accessible name. Footer icon links have no accessible names. |

---

### Table 2: Success Criteria, Level AA

| Criterion | Conformance Level | Remarks and Explanations |
|---|---|---|
| **1.2.4 Captions (Live)** | Not Applicable | No live audio or video streaming identified. |
| **1.2.5 Audio Description (Prerecorded)** | Not Evaluated | VideoPlayer component exists; no video was present on tested pages. |
| **1.3.4 Orientation** | Supports | The site is responsive and does not restrict display to a single orientation. |
| **1.3.5 Identify Input Purpose** | Partially Supports | The newsletter email field and contact form email field do not include `autocomplete` attributes. `autocomplete="email"` would assist users who rely on autofill. Name and other personal fields in the contact form also lack `autocomplete` attributes. |
| **1.4.3 Contrast (Minimum)** | Partially Supports | **Borderline pass — primary link color:** The orange link color (#ca4316, rgb(202,67,22)) on white backgrounds has an estimated contrast ratio of approximately 4.6:1, which narrowly passes the 4.5:1 threshold for normal text at AA but may fail in some themes or at smaller sizes. **Unverified — white navigation text:** Navigation links use white text over the dark navy header (#0a2a37); estimated ratio of ~13:1, which passes. **Not verified:** Many color combinations use CSS custom properties and could not be exhaustively verified through automated means. Full verification with a contrast analysis tool is needed across all page types and themes. |
| **1.4.4 Resize Text** | Supports | Text appears to use relative units. The site is responsive. Full verification at 200% zoom was not performed. |
| **1.4.5 Images of Text** | Supports | No images of text were identified on tested pages. |
| **1.4.10 Reflow** | Not Evaluated | Responsive design is present, but content reflow at 320px viewport width was not tested. |
| **1.4.11 Non-text Contrast** | Not Evaluated | Focus indicator styles and UI component boundaries were not evaluated for sufficient contrast. |
| **1.4.12 Text Spacing** | Not Evaluated | Not tested. |
| **1.4.13 Content on Hover or Focus** | Not Evaluated | Not tested. |
| **2.4.5 Multiple Ways** | Supports | Users can reach content via search, topic browsing, partner browsing, exhibitions, and primary source sets. |
| **2.4.6 Headings and Labels** | Partially Supports | **Pass:** H1 is present and descriptive on all tested pages. Heading hierarchy is generally logical. **Partial:** On the search results page, each result item uses an `<h2>` with the item title. When items share the same title (e.g., many items titled "Photography"), this results in multiple consecutive identical heading texts, which may be confusing when navigating by headings with a screen reader. This is structurally valid but represents a usability concern. |
| **2.4.7 Focus Visible** | Not Evaluated | CSS includes focus styles for skip link and form elements, but comprehensive verification across all interactive elements was not performed. |
| **3.1.2 Language of Parts** | Not Evaluated | Content from partner institutions may include non-English text. Language attribution on individual items was not evaluated. |
| **3.2.3 Consistent Navigation** | Supports | Navigation appears in consistent location and order across tested pages. |
| **3.2.4 Consistent Identification** | Supports | Components appear to be used consistently across pages (same icons, same labels). |
| **3.3.3 Error Suggestion** | Does Not Support | The contact form does not provide suggestions or guidance when field validation fails. |
| **3.3.4 Error Prevention (Legal, Financial, Data)** | Not Applicable | The site does not facilitate legal, financial, or binding transactions. |
| **4.1.3 Status Messages** | Partially Supports | **Pass:** The contact form success message uses `aria-live="assertive"` to announce success to screen reader users. **Fail:** Form validation failures are not communicated as status messages. |

---

## Revised Section 508 Report

### Chapter 3: Functional Performance Criteria

| Criteria | Conformance Level | Remarks and Explanations |
|---|---|---|
| **301.1 Without Vision** | Partially Supports | Screen reader users cannot access collection thumbnail descriptions on search/browse pages (empty alt text). DPLA logo and footer icon links lack accessible names. Video content lacks captions/transcripts. Contact form errors are not announced. Core site navigation and search functionality are partially accessible. |
| **301.2 With Limited Vision** | Partially Supports | Text resizes with the browser. Contrast ratios are borderline or unverified for some color combinations. Focus indicator visibility has not been fully verified. |
| **301.3 Without Perception of Color** | Supports | Color does not appear to be used as the sole means of conveying information. |
| **301.4 Without Hearing** | Does Not Support | The VideoPlayer component has no captions or transcript support. Users who are deaf or hard of hearing cannot access video content. |
| **301.5 With Limited Hearing** | Partially Supports | Visual alternatives to audio are not systematically provided (see VideoPlayer). |
| **301.6 Without Speech** | Not Applicable | No speech-input-required functionality was identified. |
| **301.7 With Limited Manipulation** | Partially Supports | Most functionality is keyboard-operable. Carousel navigation and at least one download control are not keyboard-accessible. |
| **301.8 With Limited Reach and Strength** | Partially Supports | Same as 301.7; keyboard access limitations affect users with limited manipulation and reach. |

---

### Chapter 5: Software (Web)

*Chapter 5 incorporates WCAG 2.0 Level A and AA by reference (502.3). See WCAG tables above. Additional Section 508 requirements not covered by WCAG:*

| Criteria | Conformance Level | Remarks and Explanations |
|---|---|---|
| **502.3 Accessibility Services** | Partially Supports | The site uses standard browser APIs and assistive technology-compatible patterns in many areas (ARIA, semantic HTML in places), but gaps exist as documented in the WCAG tables above. |
| **503.2 User Preferences** | Supports | The site does not override system accessibility preferences (e.g., reduced motion is not actively suppressed). |
| **504.2 Authoring Tool Accessibility** | Not Applicable | dp.la is not an authoring tool. |

---

### Chapter 6: Support Documentation and Services

| Criteria | Conformance Level | Remarks and Explanations |
|---|---|---|
| **602.2 Accessibility and Compatibility Features** | Does Not Support | No accessibility statement, VPAT, or documentation of accessibility features was found on dp.la or pro.dp.la. No dedicated accessibility help page exists. |
| **602.3 Electronic Support Documentation** | Not Evaluated | No formal user documentation was identified to evaluate. |
| **602.4 Alternate Formats** | Not Evaluated | Not evaluated. |
| **603.2 Information on Accessibility and Compatibility Features** | Does Not Support | No support page, FAQ entry, or documentation about accessibility features or known limitations was identified. The contact form includes a "Report an accessibility issue" subject option, which partially addresses this, but no proactive disclosure exists. |
| **603.3 Accommodation of Communication Needs** | Not Evaluated | Whether alternative communication methods (TTY, relay service) are available for users with communication disabilities was not evaluated. |

---

## Legal Disclaimer

This document was generated by an AI assistant and is provided for informational purposes only. It does not constitute legal advice, an official accessibility certification, or a warranty of conformance. DPLA makes no representations regarding the accuracy or completeness of this assessment. A formal accessibility audit by a qualified human expert is required before any compliance claims are made.

---

*VPAT® is a registered service mark of the Information Technology Industry Council (ITI).*
